"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.table = void 0;
const SheetStore_1 = require("../Framework/Asset/SheetStore");
/**
 * 注册类为表格
 * @param path 表格路径
 */
function table(path) {
    return function (ctor) {
        let store = SheetStore_1.SheetStore.getInstance();
        const name = ctor.name;
        let sheet = store.sheets.get(name);
        if (sheet !== undefined) {
            // 已经注册过了
            console.warn(`Duplicate registration data table: ${name}`);
            return;
        }
        store.sheets.set(name, { path: path });
    };
}
exports.table = table;
//# sourceMappingURL=TableDecorator.js.map