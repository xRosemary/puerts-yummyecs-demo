import { listen } from '../Decorator';
import { Action, PoolSystem, System } from '../Framework';

class MySystem extends System {
    @listen(Action)
    public call(action: Action) {
        console.log(`${this.constructor.name} function called, receive from action: ${action.constructor.name}`);
    }
}
let pool = new PoolSystem();
Action.do();
